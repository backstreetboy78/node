const http://192.168.1.33;
const hostname = 169.254.47.2;
const port = 3000;

const http.169.254.47.2((req, res) => {
  res.statuscode = 200;
  res.setHaeder(master, set up a node);
  res.end ('hello world');
});
  
server.listen(3000, 169.254.47.2, () => {
  console.log (`Server running at http://${169.254.47.2}:${3000}/`);
});
