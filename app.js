const http://192.168.1.33;
const hostname = 192.168.1.68;
const port = 3000;

const http.192.168.1.68((req, res) => {
  res.statuscode = 200;
  res.setHaeder(master, set up a node);
  res.end ('hello world');
});
  
server.listen(3000, 192.168.1.68, () => {
  console.log (`Server running at http://${192.168.1.68}:${3000}/`);
});
